@page "/"
@inject IViewInventoriesByNameUseCases _viewInventoryByNameUseCases
@inject IEditInventoryUseCase _editInventoryUseCase
<PageTitle>Test Page</PageTitle>

<ul>
@*    @foreach(var inventory in _inventories)
    {
        <li>@inventory.InventoryName</li>
        @inventory.Quantity<br/>
        @inventory.Price
    }*@
</ul>

@code{
    //private List<Inventory> _inventories = new List<Inventory>();

    //protected override async Task OnInitializedAsync()
    //{
    //    _inventories = (await _viewInventoryByNameUseCases.ExecuteAsync()).ToList();

    //    var inventory = new Inventory { InventoryId = 1, InventoryName = "Bike Seat", Quantity = 100, Price = 20 };
    //    await _editInventoryUseCase.EditExecuteAsync(inventory);
    //}
}


